<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_gravity="top"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:fillViewport="true">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_gravity="top"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center|top"
        android:orientation="vertical"
        android:padding="16dp"
        >

        <!-- Menu Toolbar -->
        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fitsSystemWindows="true">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar_home"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize" />

        </com.google.android.material.appbar.AppBarLayout>

        <!-- Layout Send -->
        <!-- Graph Info -->
        <TextView
            android:id="@+id/textview_graph_in"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center|top"
            android:padding="0dp"
            android:text="@string/kyc_info_placeholder"
            android:textSize="13sp"
            android:textStyle="italic"
            android:layout_marginBottom="10dp" />

        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/layout_of_kyc_request"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center|top"
            android:padding="10dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/frame_white"
            android:visibility="visible"
            >

            <!-- To Graph EditText -->
            <EditText
                android:id="@+id/editText_KYC_National_ID"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:hint="@string/kyc_national_id_hint"
                android:drawablePadding="2dp"
                android:drawableEnd="@drawable/baseline_hub_24"
                android:inputType="text" />

            <!-- Order EditText -->
            <EditText
                android:id="@+id/editText_KYC_Mobile_Number"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:hint="@string/kyc_mobile_number_hint"
                android:drawablePadding="2dp"
                android:drawableEnd="@drawable/baseline_shopping_cart_24"
                android:inputType="text" />

            <!-- Broadcast Button -->
            <Button
                android:id="@+id/buttonBroadcast"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="15dp"
                android:layout_marginEnd="15dp"
                android:layout_weight="1"
                android:drawablePadding="2dp"
                android:drawableEnd="@drawable/baseline_cloud_upload_24"
                android:text="@string/kyc_request_button" />

        </LinearLayout>


        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/layout_of_kyc_sign"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center|top"
            android:padding="10dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/frame_white"
            android:visibility="gone"
            >

            <!-- To Graph EditText -->
            <EditText
                android:id="@+id/EditText_KYC_MSG"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:enabled="false"
                android:hint="@string/kyc_message_hint"
                android:drawablePadding="2dp"
                android:drawableEnd="@drawable/baseline_hub_24"
                android:inputType="text" />

            <!-- Order EditText -->
            <EditText
                android:id="@+id/editText_KYC_PIN"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:hint="@string/kyc_pin_hint"
                android:drawablePadding="2dp"
                android:drawableEnd="@drawable/baseline_shopping_cart_24"
                android:inputType="text" />

            <!-- Sign Button -->
            <Button
                android:id="@+id/buttonSign"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="15dp"
                android:layout_marginEnd="15dp"
                android:layout_weight="1"
                android:drawablePadding="2dp"
                android:drawableEnd="@drawable/baseline_edit_note_24"
                android:text="@string/kyc_sign_button" />

        </LinearLayout>

        <!-- Broadcast Info -->
        <ProgressBar
            android:id="@+id/progressBar2"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:indeterminate="false"
            android:progress="0"
            />

        <TextView
            android:id="@+id/textview_broadcast_report"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp"
            android:padding="10dp"
            android:text="@string/kyc_report_placeholder"
            android:textSize="17sp"
            android:layout_marginTop="5dp"
            android:background="@drawable/frame_white"
            android:visibility="gone" />

    </LinearLayout>

</ScrollView>
